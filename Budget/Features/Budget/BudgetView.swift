import SwiftUI

struct BudgetView: View {
    var body: some View {
        PlaceholderScreen(icon: "chart.pie.fill",
                          title: "Budget",
                          phase: "Phase 4")
            .navigationTitle("Budget")
    }
}
