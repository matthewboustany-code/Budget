import SwiftUI

struct AccountsView: View {
    var body: some View {
        PlaceholderScreen(icon: "building.columns.fill",
                          title: "Accounts & Net Worth",
                          phase: "Phase 2")
            .navigationTitle("Accounts")
    }
}
