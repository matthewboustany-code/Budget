import SwiftUI

struct BillsView: View {
    var body: some View {
        PlaceholderScreen(icon: "calendar",
                          title: "Bills & Recurring",
                          phase: "Phase 5")
            .navigationTitle("Bills")
    }
}
