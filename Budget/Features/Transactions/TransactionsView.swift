import SwiftUI

struct TransactionsView: View {
    var body: some View {
        PlaceholderScreen(icon: "list.bullet.rectangle.fill",
                          title: "Transactions",
                          phase: "Phase 3")
            .navigationTitle("Transactions")
    }
}
