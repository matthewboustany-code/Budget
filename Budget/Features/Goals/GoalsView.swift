import SwiftUI

struct GoalsView: View {
    var body: some View {
        PlaceholderScreen(icon: "target",
                          title: "Goals",
                          phase: "Phase 5")
            .navigationTitle("Goals")
    }
}
